<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar" >
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>

        <item name="colorPrimary">@color/vk_color</item>
        <item name="colorPrimaryDark">@color/vk_color</item>
        <item name="colorAccent">@color/vk_color</item>
    </style>

    <!-- SUKA POPUPMENU STYLE-->
    <style name="popup_menu_style_baby" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textSize">13dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:overScrollMode">never</item>
        <item name="android:padding">3dp</item>
        <item name="android:layout_gravity">center</item>
    </style>

</resources>
